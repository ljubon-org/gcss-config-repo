name: "Drift Check"

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  drift-check:
    secrets:
      tfc_token: ${{ secrets.TFC_TOKEN }}
    uses: ljubon-org/github-terraformer/.github/workflows/drift-check.yaml@test-env
    with:
      gcss_ref: test-env
      tfc_org: "ljubo-demo" # FIXME: now
