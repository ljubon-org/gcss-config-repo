name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: ljubon-org/github-terraformer/.github/workflows/bulk-import.yaml@test-gcss-1135
    with:
      gcss_ref: test-gcss-1135
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
